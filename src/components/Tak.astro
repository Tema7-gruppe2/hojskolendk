---
import Button from '../components/Button.astro';
---

<section>
    <div class="container">
        <div class="subtitle">Online infomøde</div>
        <h1 class="title">Tak for din tilmelding</h1>
        <div class="text-block">
            <p>
                Du vil modtage en bekræftelse og et link til online mødet på email
                inden for de næste 5 minutter.
            </p>
            <p class="normal">
                Hvis ikke du modtager en mail, så tag kontakt til os på tlf.
                <span class="highlight">7027 9007</span>
                eller
                <a href="mailto:info@hojskolendk.dk" class="highlight">
                    info@hojskolendk.dk
                </a>.
            </p>
            <p>
                Vi glæder os til at møde dig!
            </p>
        </div>
    </div>
</section>

<style>
    .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 80px 20px;
        text-align: center;
    }

    .subtitle {
        font-family: var(--font-secondary);
        color: var(--color-text-secondary);
        font-size: 1.2rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
    }

    .title {
        font-family: var(--font-primary);
        color: var(--color-text-primary);
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 1.5rem;
    }

    .text-block p {
        font-family: var(--font-secondary);
        color: var(--color-text-primary);
        font-size: 1rem;
        margin-bottom: 1.5rem;
    }

    .highlight {
        color: var(--color-primary);
        font-weight: bold;
    }